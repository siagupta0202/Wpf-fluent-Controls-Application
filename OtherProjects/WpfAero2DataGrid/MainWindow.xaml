<Window x:Class="WpfAero2DataGrid.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAero2DataGrid"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="24" FontWeight="Bold" Text="DataGrid" Margin="0 0 0 10"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button x:Name="Aero2" Content="Aero2" Click="Aero2_Click"  Margin="10"/>
            <Button x:Name="Light" Content="Light" Click="Light_Click"  Margin="10"/>
            <Button x:Name="Dark" Content="Dark" Click="Dark_Click"  Margin="10"/>
        </StackPanel>
        <DataGrid Grid.Row="2" x:Name="dataGrid" Margin="10" />
    </Grid>
</Window>
